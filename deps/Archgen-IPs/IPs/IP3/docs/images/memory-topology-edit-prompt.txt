Use case: precise-object-edit. Edit target: attached IP3 block diagram. Preserve the white background, crisp navy lettering, orange NVDLA additions, all labels and all right-side NVDLA/control/peripheral blocks. Correct only the LEFT memory topology to match emitted hardware: SBUS connects downward to Shared inclusive L2 512 KiB, which connects downward to a NEW BLUE existing-hardware box 'Memory bus (MBUS)'. MBUS branches to BOTH '64 KiB scratchpad · 0x08000000' and '64-bit AXI4 memory port'. The external memory system box, outside ChipTop, connects ONLY to that AXI4 port, and reads '256 MiB address window at 0x80000000'. Remove the current direct SBUS-to-scratchpad connection. Reposition the two memory endpoints side by side below MBUS in the left half, using the available white space without overlap. Keep L2 below SBUS. Keep MBUS below L2 and both endpoints below MBUS. All memory path connectors should be bidirectional. No direct L2-to-AXI shortcut bypassing MBUS. Keep the diagram's title, CPU/caches, orange new blocks and interrupt line, all other content unchanged. The scratchpad and MBUS are existing blocks colored blue, never orange. Make text readable, no line/text overlaps.
