Edit this diagram only to remove the dark blurred gradient background and all glows/shadows. Replace all background outside the blocks with solid opaque pure white (#FFFFFF), including the title and footer regions, so every navy title and footer label is clearly readable. Preserve every block, every number, every connection, every word and the layout exactly. Keep block fills pale teal and pale orange, dark navy text, orange highlights, flat engineering diagram styling. No dark background, no gradient, no translucency, no glow. Improve text contrast everywhere.
